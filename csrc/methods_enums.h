#ifndef METHODS_ENUM_H
#define METHODS_ENUM_H

// Define the enum
typedef enum {
    ORIGINAL = 0,
    HUFFMAN = 1,
    ZSTD = 2,
    TRUNCATE = 3,
    FSE = 4
} MethodsEnums;

#endif // METHODS_ENUM_H
 
